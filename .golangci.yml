# NOTE: errcheck, ineffassign, and staticcheck are disabled for now
# due to many pre-existing violations. Enable them incrementally as
# the codebase is cleaned up.
linters:
  default: standard
  disable:
    - errcheck
    - ineffassign
    - staticcheck

formatters:
  enable:
    - gofmt
